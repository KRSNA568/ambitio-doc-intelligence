# Copy to .env and fill in, or just `export GEMINI_API_KEY=...` in your shell.
GEMINI_API_KEY=your-gemini-api-key-here

# Optional overrides (sensible defaults exist in code):
# GEMINI_MODEL=gemini-2.5-flash       # use gemini-2.5-pro for higher quality
# CHROMA_DIR=backend/.chroma
# RULES_DB_PATH=backend/operator_rules.db
